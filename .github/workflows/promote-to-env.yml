name: Promote to environment

# Gated GitOps promotion: the tag is NOT entered by hand. It is read from the
# previous environment's overlay and copied forward, so an env can only ever
# receive a tag that already ran in the env before it:
#   dev --> stage --> prod        (sandbox promotes from dev as a side env)
# The build/CI pipeline is what pins dev; this workflow promotes onward.

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment to promote to"
        required: true
        type: choice
        options:
          - stage
          - sandbox
          - prod

jobs:
  promote:
    runs-on: ubuntu-latest
    # Uses GitHub Environment protection rules if configured (e.g. require a
    # reviewer for 'prod'). Auto-created without protection if not set up.
    environment: ${{ github.event.inputs.environment }}
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Resolve source environment
        id: resolve
        run: |
          TARGET="${{ github.event.inputs.environment }}"
          case "$TARGET" in
            stage)   SOURCE="dev"   ;;
            prod)    SOURCE="stage" ;;
            sandbox) SOURCE="dev"   ;;
            *) echo "::error::Unknown target environment: $TARGET"; exit 1 ;;
          esac
          echo "source=$SOURCE" >> "$GITHUB_OUTPUT"
          echo "Promotion path: $SOURCE -> $TARGET"

      - name: Read tag from source overlay
        id: tag
        run: |
          SOURCE="${{ steps.resolve.outputs.source }}"
          SRC_FILE="repo/overlay/${SOURCE}/kustomization.yaml"
          if [[ ! -f "$SRC_FILE" ]]; then
            echo "::error::Source overlay not found: $SRC_FILE"; exit 1
          fi
          TAG="$(grep 'newTag:' "$SRC_FILE" | head -1 | awk '{print $2}' | tr -d '"')"
          if [[ -z "$TAG" ]]; then
            echo "::error::No newTag found in $SRC_FILE"; exit 1
          fi
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "Tag to promote (from $SOURCE): $TAG"

      - name: Update target overlay
        id: update
        run: |
          TARGET="${{ github.event.inputs.environment }}"
          TAG="${{ steps.tag.outputs.tag }}"
          DST_FILE="repo/overlay/${TARGET}/kustomization.yaml"
          if [[ ! -f "$DST_FILE" ]]; then
            echo "::error::Target overlay not found: $DST_FILE"; exit 1
          fi

          CURRENT="$(grep 'newTag:' "$DST_FILE" | head -1 | awk '{print $2}' | tr -d '"')"
          if [[ "$CURRENT" == "$TAG" ]]; then
            echo "Target $TARGET is already on $TAG — nothing to promote."
            echo "changed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          sed -i "s|newTag:.*|newTag: \"${TAG}\"|" "$DST_FILE"
          echo "changed=true" >> "$GITHUB_OUTPUT"
          echo "Updated $DST_FILE: $CURRENT -> $TAG"
          cat "$DST_FILE"

      - name: Commit and push
        if: steps.update.outputs.changed == 'true'
        run: |
          TARGET="${{ github.event.inputs.environment }}"
          SOURCE="${{ steps.resolve.outputs.source }}"
          TAG="${{ steps.tag.outputs.tag }}"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add "repo/overlay/${TARGET}/kustomization.yaml"
          git commit -m "promote: ${SOURCE} -> ${TARGET} image tag -> ${TAG}"
          git push
