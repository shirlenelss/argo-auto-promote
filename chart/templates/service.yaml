apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}
  labels:
    app: {{ .Values.name }}
spec:
  selector:
    app: {{ .Values.name }}-app
  ports:
    - port: 80
      targetPort: {{ .Values.port }}
      protocol: TCP
  type: ClusterIP
