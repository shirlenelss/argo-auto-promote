# Host is a placeholder; overlays patch spec.hosts[0] to the env-specific URL.
apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: service-b
spec:
  hosts:
    - service-b.PLACEHOLDER
  gateways:
    - sample-gateway
  http:
    - route:
        - destination:
            host: service-b
            port:
              number: 80
