apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-a-app
  labels:
    app: service-a-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-a-app
  template:
    metadata:
      labels:
        app: service-a-app
    spec:
      containers:
        - name: service-a-app
          image: service-a:0.0.1
          ports:
            - containerPort: 8080
          env:
            # IMAGE_TAG's value is overwritten per overlay by a
            # `replacements` block that reads it back off this same
            # container's own (already-promoted) image field, so it
            # never needs to be set by hand.
            - name: ENVIRONMENT
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: IMAGE_TAG
              value: unset
            # Bare Service name resolves via cluster DNS within the same
            # namespace, so no per-env templating is needed here.
            - name: SERVICE_B_URL
              value: http://service-b
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 512Mi
